import styled from '@emotion/styled'

const Container = styled.main`
  width: 400px;
  margin: 0 auto;
  text-align: center;
`

const List = styled.ul`
  width: 100%;
`

export default {
  Container,
  List,
}
